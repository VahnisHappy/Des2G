
export class GeocodingService {
    // private static _instance: GeocodeService

    public static getInstance() {
        // if (!GeocodingService._instance)
        //     GeocodingService._instance = Geocoding(})
        // return GeocodingService._instance
    }
}